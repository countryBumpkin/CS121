//
// Created by Garrett Wells on 2019-03-01.
//

#include "linkedlist.cpp"
#include <iostream>
using namespace std;

int CountOdd(NodePtr head);

int main()
{
    CountOdd(NodePtr head);
}

int CountOdd(NodePtr head)
{
    numOdd = 0;
    if(head == NULL)
    {
        return 0;
    }
    else
    {
        do
        {
            if(head % 2 = 0)
                numOdd = numOdd;
                n->next;
                else
                    numOdd++;
        }while(head != NULL);

    }
}